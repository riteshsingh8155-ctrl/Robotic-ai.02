      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies  <-- YE NAYA STEP ADD KARO
        run: |
          python -m pip install --upgrade pip
          pip install smartapi-python pyotp python-telegram-bot requests

      - name: Run Python script
        run: python Main.pyname: Run Angel Bot

on:
  workflow_dispatch:  # Haath se run karne ke liye button
  schedule:
    - cron: '*/15 * * * *'  # Har 15 min me auto chalega

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Code Download
        uses: actions/checkout@v4

      - name: Python Setup
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Packages
        run: pip install -r requirements.txt

      - name: Run Bot
        env:
          API_KEY: ${{ secrets.API_KEY }}
          CLIENT_CODE: ${{ secrets.CLIENT_CODE }}
          ANGEL_PASSWORD: ${{ secrets.ANGEL_PASSWORD }}
          TOTP_SECRET: ${{ secrets.TOTP_SECRET }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python Main.py
